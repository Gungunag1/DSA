class Solution {
public:
    int longestSubsequence(vector<int>& nums) {
        int n=nums.size();
        int ans=0;
        int cnt=0;
        for(int x:nums){
            ans^=x;  
        }
        if(ans!=0){
            return n;
        }
        for(int x:nums){
            if(x!=0){
               return n - 1; 
            }
        }

        return 0;
    }
};
